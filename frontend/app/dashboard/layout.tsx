/**
 * Dashboard Layout
 * 
 * FR-4.1: Layout для дашборда (header, sidebar)
 */

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

